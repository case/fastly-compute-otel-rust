#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

# Run lints first
echo "=== Lint ==="
./bin/lint

echo ""
echo "=== Test ==="

# Native unit tests (serialization, logic — no WASI runtime needed)
cargo test

echo ""
echo "All checks passed."
